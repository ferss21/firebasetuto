# Ejemplos Firebase + Python (Firestore)

Este proyecto contiene scripts simples en Python para probar las operaciones
más importantes de Firestore usando Firebase Admin SDK.

## Requisitos

1. **Python 3** instalado.
2. Clave de cuenta de servicio de Firebase (service account) en un archivo:
   `serviceAccountKey.json` (NO subir a repositorios públicos).

### 1. Crear entorno virtual

```bash
py -m venv venv
